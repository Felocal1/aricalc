# Dependências da versão WEB da AriCalc.
# Para instalar em um ambiente virtual (recomendado):
#
#     python -m venv .venv
#     .\.venv\Scripts\python -m pip install -r requirements.txt
#
# O modo terminal (main.py) usa apenas a biblioteca padrão do Python
# e não precisa de nada disso.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0